version: '3'
services:
        stepik154:
                build:
                        context: .
                ports:
                        - 9080:80
                volumes:
                        - .:/home/box/web
